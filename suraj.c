#include<stdio.h>
//int main()
//{
    int sum = 544;
  //   return 0;
//}

// Note- We will comment out main because main is defined two that's why we comment out and then our code will run properly

//Error will come if we define main two times

// Output:-
// The sum is 944

// combine with storage.c